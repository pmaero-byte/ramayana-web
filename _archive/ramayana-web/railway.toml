# Railway runtime config — Node 22, serves static game from /app
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.mjs"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
